/**
 * operaciones
 * Juan Carlos jurado
 * 5 abril 2024
 * version 1.0
 */


function contador(numero){
   let   contador;
   const  numer = numero;
   for(contador=1;contador < numer;contador++){
      console.log(contador);
   }
  return contador;

   
}
const contad=function contador(){
   let   contador;
   const  numero;
   for(contador=1;contador < numero;contador++){
      console.log(contador);
   }
  return contador;

   
}